import React from 'react';
import {Typography} from "@mui/material";

export default function Home() {
    return (
        <Typography variant="body1">Use the options on the navigation bar to continue.</Typography>
    );
}